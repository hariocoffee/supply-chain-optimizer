# Database Configuration
DATABASE_URL=postgresql://optimizer:CHANGE_ME_SECURE_PASSWORD@postgres:5432/optimizer_db
POSTGRES_DB=optimizer_db
POSTGRES_USER=optimizer
POSTGRES_PASSWORD=CHANGE_ME_SECURE_PASSWORD

# Redis Configuration
REDIS_URL=redis://redis:6379/0

# Airflow Configuration
AIRFLOW_URL=http://airflow-webserver:8080
AIRFLOW_USERNAME=admin
AIRFLOW_PASSWORD=CHANGE_ME_SECURE_PASSWORD

# Ollama Configuration
OLLAMA_URL=http://ollama:11434
OLLAMA_MODEL=qwen2.5:0.5b

# Application Configuration
STREAMLIT_PORT=8501
ENVIRONMENT=development
DEBUG=True

# Security Configuration
SECRET_KEY=CHANGE_ME_GENERATE_SECURE_SECRET_KEY

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=logs/app.log

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace all CHANGE_ME_* values with secure passwords
# 3. Generate a secure SECRET_KEY: python -c "import secrets; print(secrets.token_urlsafe(32))"
# 4. For production, use strong passwords (16+ characters, mixed case, numbers, symbols)
